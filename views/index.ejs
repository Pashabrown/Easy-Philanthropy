<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8" />
    <title></title>
</head>

<body>
    <nav>
        <a href="/index/new">Create a New Non Profit</a>
    </nav>
    <h1>Non Profits Main page</h1>
    <ul>
        <% for (let i=0; i < nonprofits.length; i++){ %>
            <li>
                The
                <a href="/nonprofits/<%= nonprofits[i].id %>">
                    <%= nonprofits[i].id; %>
                </a>
                is <%= nonprofits[i].name; %>
                    <% if (nonprofits[i].iHaveDonated===true){ %> I
                        have donated. <% } else { %> I have not donated. <% } %>
                                <a href="/nonprofits/<%=nonprofits[i].id; %>/edit">Edit</a>
                                <form action="/nonprofits/<%= nonprofits[i].id %>?_method=DELETE" method="POST">
                                    <input type="submit" value="DELETE" />
                                </form>
            </li>
            <% } %>
    </ul>
</body>

</html>