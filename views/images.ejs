<!DOCTYPE html>
<html lang="en">
<!-- THE HEAD TAG PARTIAL -->
<%- include("partials/head") %>

    <body>
        <!-- THE HEADER TAG PARTIAL -->
        <%- include("partials/header") %>
        <main>
            <!-- *********DISPLAY OUR GOALS********* -->
            <h1 class="title">My Images</h1>
            <a href="/auth/logout"><button class="button">Logout</button></a>
            
            <!-- **********NEW GOALS FORM**************** -->
            <form action="/images" method="post">
                <input type="text" placeholder="new image" name="url" />
                <input type="submit" value="add image" />
            </form>
            <% for (image of images) { %>
                <img src="<%=image.url%>" alt="">
                <% } %>
                <nav>
                    <a href="/images/new">Create a new NonProfit</a>
                </nav>
                <%for ( i = 0; i < images.length; i++) { %>
                <form action="/images/<%= images[i].id %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE" />
                    <% } %>
                </form>
            </main>
            <!-- THE HEAD TAG PARTIAL -->
            <%- include("partials/footer") %>


    </body>

</html>